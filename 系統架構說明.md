# 系統架構說明 — 補貨儀表板 & 流量分析

> 更新日期：2026-08-10（v3：SZ 倉重構為美國倉/佳樂倉 + ASIN 為 Key）

---

## 整體架構

```
Amazon SP-API
     ↓
  n8n (port 5678)  — 排程觸發 / 手動觸發
     ↓  HTTP POST  (http://etl:8000)
  FastAPI ETL (port 8000)  — Python service
     ↕
  DuckDB (embedded)  — ./duckdb/sp_api.duckdb
     ↑
  Browser  — GET /dashboard → dashboard.html
```

- 所有服務跑在同一個 Docker network `sp_network`
- n8n 用內部 hostname `http://etl:8000` 呼叫 FastAPI（不走外網）
- DuckDB 是 embedded，直接在 FastAPI process 內讀寫，不需要獨立 server

---

## n8n Workflows（7 個）

| Workflow | 排程 | 主要步驟 |
|---|---|---|
| `awd_replenishment_workflow` | 每日 06:00 UTC（台灣 14:00）+ 手動 | ① FBA 庫存同步 → ② AWD 庫存同步 → ③ 訂單同步 → ④ Sales 報告同步 → ⑤ 庫存警示 → 📧 Email 通知 |
| `workflow_traffic` | 每月 2 日 03:00 UTC | POST /etl/traffic/sync → Wait 15min → Poll → Count |
| `workflow_traffic_mtd` | 每日 09:00 UTC | POST /etl/traffic/sync-mtd → Poll（月初 1 號自動跳過）|
| `workflow_orders` | 每 4 小時 | POST /etl/orders/sync → Wait 30s → Check status |
| `workflow_finance` | 每日 23:00 UTC（台灣 07:00）| POST /etl/finance/sync → Wait 150s → Check status |
| `workflow_ads` | 每日 00:00 UTC（台灣 08:00）| POST /etl/ads/sync → Wait 3min → Check status |
| `workflow_fees` | 每月 2 日 04:00 UTC | POST /etl/fees/sync → Poll（FBA fee 費率更新）|
| `workflow_traffic_backfill` | 手動 | POST /etl/traffic/backfill（指定月份範圍，最多 24 個月）|

---

## 補貨儀表板（📦 Tab）

### API 端點
| 用途 | 方法 | 端點 |
|---|---|---|
| 讀取補貨資料 | GET | `/replenishment` |
| 匯出 CSV | GET | `/replenishment/export` |
| 上傳產品目錄 | POST | `/etl/catalog/upload` |
| 上傳深圳倉庫存（含美國倉/佳樂倉/Unit-Case）| POST | `/etl/sz/upload` (預設 replace=true) |
| 下載 SZ Upload Template（依 Anchor 生成）| GET | `/etl/sz/template` |
| 查詢 SZ 狀態（含資料新鮮度 days_old）| GET | `/etl/sz/status` |
| SZ SKU 對不到 Anchor 清單 | GET | `/etl/sz/unmatched` |
| 同步 AWD 庫存 | POST | `/etl/awd/sync` |
| 查詢 AWD 狀態 | GET | `/etl/awd/status` |
| 儲存 SKU 設定 | POST | `/replenishment/sku-config` |
| 儲存水位參數 | POST | `/replenishment/controls` |

### 使用的 DuckDB 資料表
- `inventory` — FBA Available / Inbound
- `awd_inventory` — AWD Available / Inbound / Outbound
- `sz_warehouse` — 深圳倉庫（`us_qty` 美國倉、`jl_qty` 佳樂倉、`unit_per_case` 箱規）
- `product_catalog` — SKU ↔ ASIN / Collection / Name / COG / FBA fee
- `sku_config` — 產品類型（流量款/一般款）/ 箱規 / ETA
- `sales_summary` — 每日出貨 Units（計算銷速用）
- `replenishment_controls` — 補貨水位參數（動態調整）

### JOIN 邏輯（v3 — ASIN 為 Key）
**所有 Amazon 相關資料以 ASIN 為 Key 聚合**，解決同一 ASIN 有多個 seller SKU 的問題（例：SKU 換版、`-stickerless`、`Amazon.Found.XXX` 等）：

```
sku_asin CTE = Anchor ∪ FBA ∪ Sales 的 SKU→ASIN 對應
├─ fba_by_asin    ← inventory GROUP BY asin
├─ awd_by_asin    ← awd_inventory JOIN sku_asin GROUP BY asin
├─ sales_by_asin  ← sales_summary GROUP BY asin
├─ us_by_asin     ← sz_warehouse.us_qty JOIN sku_asin GROUP BY asin
├─ jl_by_asin     ← sz_warehouse.jl_qty JOIN sku_asin GROUP BY asin
└─ uc_by_asin     ← sz_warehouse.unit_per_case JOIN sku_asin GROUP BY asin
```

當同一 ASIN 對應多個 SKU 時，Dashboard SKU 欄會顯示 `⚠️ N SKUs` 徽章，並在 tooltip 列出所有 alias SKU。

### 補貨計算邏輯（全在後端 replenishment router）
- **月均銷量** = sales_summary 近 30 天 units（依 ASIN 聚合）
- **FBA Coverage** = (FBA Available + Inbound) / 月均銷量
- **AWD Coverage** = (AWD Available + Inbound - Outbound) / 月均銷量
- **美國倉 Month** = us_qty / 月銷量
- **深圳倉庫總量 Month** = (us_qty + jl_qty) / 月銷量
- **空運警示** = Total Coverage < (生產天數 + 海運天數) / 30
- **3M Cap 警示** = FBA + AWD Coverage > total_cap_months

### 返單計畫（v4 — 佳樂→美國 移倉 + 工廠→美國 返單 + 欠數）
物流路線：`工廠 ─返單(≈45天)─→ 美國倉 ← ─移倉(即時)─ 佳樂倉`

**欠數 (`pending_qty`)** = 已向工廠下單、但工廠尚未交付的 PO 數量。工廠交貨後 user 手動歸零。避免重複下單。

- **安全庫存門檻** = 月銷量 × `sz_transfer_level`（動態依產品類型）
- **美國虛擬庫存** = 美國倉現貨 + 欠數（將自動入美國倉的量）
- **美國倉缺口** = max(0, 安全庫存門檻 − 美國虛擬庫存)  ← 用虛擬庫存扣，避免重複下單
- **移倉數建議** = min(佳樂倉, 美國倉缺口)  ← 加佳樂倉上限
- **建議返單數量** = max(0, 美國倉缺口 − 佳樂倉)  ← 佳樂倉也不夠時才向工廠下單
- **美國倉 Month** = 美國倉現貨 / 月銷量（純現貨）
- **美國虛擬 Month** = 美國虛擬庫存 / 月銷量（含欠數）
- **深圳倉庫總量 Month** = (美國倉 + 佳樂倉) / 月銷量（不含欠數，因為欠數不在物理倉）
- **Total Pipeline** = (FBA total + AWD total + 美國倉 + 佳樂倉) / 月銷量（**不含欠數**）
- **返單警示（三級）**：
  - 🔴 `需返單`：建議返單數量 > 0
  - 🟡 `佳樂告急`：移倉後佳樂倉剩餘 < 安全庫存門檻 × 0.5
  - 🔵 `已下單待收`：欠數 > 0 且不需再下單
- **整箱返單量** = ⌈建議返單數量 ÷ Unit/Case⌉ × Unit/Case（實務下工廠訂單用）
- **保守可撐天數** = (美國倉 + 佳樂倉) ÷ (月銷量 ÷ 30) — 不含欠數，防斷貨
- **交期倒數 (Option C)** = 保守可撐天數 − `production_days`
  - 正 → 「+X 天」（可再等）
  - 0 → 「今天！」
  - 負 → 「⚠️ 延遲 X 天」（已來不及）
- **建議下單日** = today + max(0, 交期倒數)（延遲時顯示今天，UI 標紅）

### Controls（水位參數）
| Key | 預設 | 說明 |
|---|---|---|
| `production_days` | 30 | 生產天數 |
| `sea_days` | 40 | 海運天數 |
| `awd_target_normal` / `_hero` | 1.0 / 1.5 | AWD 目標月數 |
| `total_cap_months` | 3.0 | FBA+AWD 總上限月數 |
| `sz_level_normal` / `_hero` | 1.0 / 1.5 | SZ 返單水位（廠端庫存最低月數）|
| `sz_transfer_level_normal` / `_hero` | 1.0 / 1.5 | **SZ 移倉水位 — 美國倉應保持的月數**（新）|

### 深圳倉資料流（v3）
1. **下載 Template**：Dashboard 點「⬇️ 下載 Template」→ `GET /etl/sz/template` → 自動由 Anchor 產出 xlsx（含所有 SKU + 空白庫存欄）
2. **填數字**：使用者填 `美國倉庫存` / `佳樂倉庫存` / `Unit/Case`
3. **上傳**：點「⬆️ 上傳」→ `POST /etl/sz/upload?replace=true` → 完全取代 sz_warehouse，同步更新 sku_config.unit_per_case
4. **過期警示**：`/etl/sz/status` 若 `days_old > 7` 回傳 `is_stale=true`，Dashboard 顯示黃色橫幅提醒

---

## 流量分析（📊 Tab）

### API 端點
| 用途 | 方法 | 端點 |
|---|---|---|
| 讀取 Dashboard 彙整資料 | GET | `/etl/traffic/data` |
| 觸發月報同步 | POST | `/etl/traffic/sync` |
| 觸發當月 MTD 同步 | POST | `/etl/traffic/sync-mtd` |
| 觸發歷史回補 | POST | `/etl/traffic/backfill` |
| 查詢同步狀態 | GET | `/etl/traffic/status` |
| 查詢歷史執行紀錄 | GET | `/etl/traffic/history` |

### 使用的 DuckDB 資料表
- `sales_traffic` — 流量 + 銷售 by Child ASIN（月報 / MTD）
- `product_catalog` — JOIN 取得 SKU / Collection / Name

### 前端彙整邏輯（全在 dashboard.html JS）
後端 `/etl/traffic/data` 只吐 raw detail（每月 × SKU），前端負責：
- 月趨勢（Sessions / CVR / Revenue / Units）
- by Collection / by Name 排行（Pie charts）
- Sessions & CVR 分拆 panel
- SKU 成長衰退分析（Quadrant + 比較表）

---

## DuckDB 資料表總覽（10 張）

| 資料表 | 來源 | 用途 |
|---|---|---|
| `product_catalog` | 手動上傳 Anchor | SKU ↔ ASIN / Collection / Name，**補貨 + 流量共用** |
| `inventory` | SP-API FBA | FBA 現貨 + 在途 |
| `awd_inventory` | SP-API AWD | AWD 現貨 + 在途 |
| `sz_warehouse` | 手動上傳 Template | 美國倉 + 佳樂倉庫存 + Unit/Case |
| `sales_summary` | SP-API Reports | 每日出貨量（補貨計算用） |
| `sku_config` | Dashboard 設定 | 產品類型 / 箱規 / ETA |
| `replenishment_controls` | Dashboard 設定 | 補貨水位參數 |
| `sales_traffic` | SP-API Business Report | 流量 + 銷售（流量分析用）|
| `orders` | SP-API Orders | 訂單明細 |
| `ads_sponsored_products` | SP-API Ads | 廣告每日摘要 |

---

## 手動操作項目（不透過 n8n）

| 操作 | 方式 |
|---|---|
| 上傳 MAGEASY Anchor（產品目錄）| Dashboard 上傳 UI → POST /etl/catalog/upload (預設 replace=true)|
| 上傳深圳倉庫存 | Dashboard **⬇️ 下載 Template** → 填數字 → **⬆️ 上傳** (預設 replace=true) |
| 設定 SKU 類型 / 箱規 / ETA | Dashboard SKU 設定 Modal |
| 調整補貨水位參數 | Dashboard 水位設定 Modal |
| 回補歷史流量資料 | n8n workflow_traffic_backfill 手動觸發 |
