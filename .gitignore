# ── 敏感資訊（絕對不能上傳）──────────────────────
.env
*.env

# ── 資料庫（本機資料，不同步）────────────────────
duckdb/
*.db
*.db.wal

# ── n8n 資料（包含 credentials）──────────────────
# 用 n8n/* 而非 n8n/ —— 排除整個資料夾後，Git 無法再用 ! 加回裡面的檔案
n8n/*
# 例外：workflow JSON 檔要版本控制（不含機密，只有 placeholder credential id）
!n8n/*.json

# ── LibreOffice / Excel 暫存鎖定檔 ──────────────
.~lock.*

# ── Python ───────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── macOS ────────────────────────────────────────
.DS_Store

# ── 其他 ─────────────────────────────────────────
*.xlsx
*.csv
